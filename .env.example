# Local secrets for `wrangler dev` — copy to `.dev.vars`
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
OPENAI_API_KEY=sk-...
# Local override (production uses https://health.emailmenow.com from wrangler.jsonc)
APP_URL=http://localhost:8787
# Amazon Associates Store ID / tracking ID (e.g. yourstore-20) — public, not a secret
AMAZON_ASSOCIATE_TAG=
